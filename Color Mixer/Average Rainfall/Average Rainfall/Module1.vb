Module Module1

    Sub Main()
        Dim year = 0
        Dim months = 0
        Dim rainfall = 0
        Dim avgRain = 0
        Dim totalrainfall
        Dim inches

        'Retrieve Data and record average rainfall
        Console.Write("Input the number of years recorded:")
        year = CInt(Console.ReadLine())

        'For Next years = 1 To Max_VaL
        'For next months = 1 To 12
        Console.Write("Enter the rainfall (in inches) for the month:")
        rainfall = CInt(Console.ReadLine())
        totalrainfall = inches + rainfall

    End Sub

End Module
